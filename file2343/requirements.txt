# ── Core ML ───────────────────────────────────────────────────────────────────
# CPU-only torch keeps the image under 2 GB.
# For GPU support, remove the +cpu suffix and the --extra-index-url line.
torch==2.1.2+cpu
torchvision==0.16.2+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# ── Vision / video ────────────────────────────────────────────────────────────
opencv-python-headless>=4.8.0
Pillow>=10.0.0
numpy>=1.24.0

# ── Web server ────────────────────────────────────────────────────────────────
Flask>=3.0.0
Werkzeug>=3.0.0

# ── Training / evaluation (optional — only needed if re-training inside container)
scikit-learn>=1.3.0
tqdm>=4.65.0
matplotlib>=3.7.0
